set_slot("test1", 
    Method clone("Hello World from a method!" println));
test1;

set_slot("test2",
    Method clone(what, additional,
        "Hello " +(what) println
    ));
test2("World");

set_slot("magic",
    Method clone(foo,
        bar println
    )
);
get_slot("magic") argument_names set_at(0, "bar");

magic("Maaaaaagic!");
